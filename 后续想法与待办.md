# 后续想法与待办

一期已经上线（两个站 + Cloudflare Workers + 自定义域 + GSC）。这份文档记「以后想做、现在先不做」的东西，免得散在聊天记录里丢掉。

每条都写清楚：**为什么想做、怎么做、有什么坑、要动到哪里**。真正开工前照这份判断，别凭一句话的印象就动手。

参考来源：`https://ipgs.cn/md/`（一个 Markdown → 优雅长图的工具，2026-08 看的）。它跟我们不是同一品类（它做美化分享，我们做格式转换），但有两点值得借。

---

## 1. 「示例内容」按钮（低成本，纯加分，随时可做）

**是什么**：粘贴框旁边加一个「示例 / Try an example」按钮，点一下把一段真实内容填进输入框。参考站有这个，我们现在只有 placeholder 占位文字 —— 一点就没、还得用户自己想输入什么。

**为什么值得做**：
- 我们做的是全球 SEO 长尾。搜「markdown to html」进来的人很多是「来试试看效果」的，手边不一定有文件。一个填好示例的空状态，能把「我看看」直接变成「哦原来是这样」。
- 每个工具页的示例内容还能顺便展示该格式的**特有能力**（方案 §8.7 本来就要求每页有格式特有内容）——Markdown 页示例带 GFM 表格 + 任务列表 + 删除线，CSV 页带能触发分隔符自动识别的数据，等等。

**怎么做**：
- 示例文本本身是 Markdown / CSV / HTML 源码，**不用翻译**（跟 `<pre>` 样例同理，六语言逐字相同）。只有按钮文案「示例 / Try an example」进字典、六语言。
- 数据放哪：跟着 `TOOL_INPUT` 或字典走，一个 engine 一段。别为每个页面手写一份散落的字符串 —— 参考现有 `acceptSummary` / `pagesForExtension` 那种「从一处派生」的做法。
- 只在**有粘贴框**的页面加（`paste !== "none"`）。DOCX / Excel / PDF 这种纯文件页没有输入框，示例无处可填 —— 那些页真要给示例得另想（比如放一个可下载的样例文件），第一版先不管。
- 点「示例」= 往输入框填内容 + 触发一次转换，顺带复用刚做的「自动滚到结果区」。

**坑 / 要动到哪里**：
- `converter.tsx`（两个站）：加按钮 + 填充逻辑。
- 六个字典 + `i18n/types.ts`：加按钮文案 key。
- `verify/i18n.mjs`：示例正文如果进了 DOM，会被漏翻检查逮到 —— 得像 `<pre>` 一样排除，或确认它本来就在 `<textarea>`/`<pre>` 里（那两个已经被排除）。**动之前先跑一遍 verify:i18n 看会不会误报。**
- 别破坏「无外部请求」：示例是内置字符串，不从网上拉。

**规模**：小。半天内能两个站都做完 + 测试。

---

## 2. Markdown → 优雅长图 / 导出图片（大，是「另一个产品」，要单独决策）

**是什么**：把转换结果渲染成一张适合手机分享的长图（发朋友圈 / 小红书 / 公众号）。参考站的核心差异化就是这个（「一键保存手机超清长图」）。

**为什么诱人**：中文圈「把文档转成好看的长图发出去」需求很大，而且能纯客户端做（html-to-image / html2canvas 之类），不破坏「文件不上传」。

**为什么现在不做 / 要想清楚**：
- **它是另一个产品，不是现有转换器上的一个按钮。** 我们的定位是「干净、快、隐私」的格式转换；长图是「美化 + 分享」，用户心智不同。硬塞进现有工具页会稀释定位。
- **依赖体积**。html-to-image 类的库是几百 KB。而我们整站的卖点之一就是包小、每页只加载自己要用的解析器（方案 §8：首页一个解析器都不该加载）。塞进现有页直接违背这条。
- **市场错位**。我们目标是英文全球；长图分享主要是中文社交场景。两者不完全重合。
- **字体 / CJK 渲染**。长图要好看得内嵌字体，html2canvas 对 CJK 字体、emoji、复杂排版的还原是老大难，做不好比不做糟。

**如果要做，更干净的路子**：
- **开第三个站**（比如 `md 转长图` / 某个新域名），而不是往 docstomd / docs2html 里塞。复用现有的 monorepo 结构、i18n、部署流水线、设计系统底子，但产品定位独立、依赖独立、包体独立。
- 先验证需求再投入：这是战略决定，不是一个功能。开工前值得先看看关键词量、竞品（参考站、其他长图工具）活得怎么样。

**坑 / 要动到哪里**（真做的时候）：
- 新 app（`apps/xxx`）、新域名、新 Worker + wrangler.jsonc（照 docstomd 那套）。
- 客户端图片渲染库选型 + 体积预算。
- 字体自托管（跟现在 pdf.js / iconify 自托管同理，「断网可用」不能破）。
- 设计：别学参考站那种居中卡片 + 蓝紫的通用 SaaS 长相 —— 那正是 AGENTS.md 刻意避开的三种 AI 默认长相之一。新站要有自己的签名。

**规模**：大。按「新站」立项，不是「加功能」。

---

## 明确不学的

参考站整体是「居中、卡片、蓝紫感」的通用 SaaS 长相，撞在 AGENTS.md 点名要避开的三种 AI 默认长相上。我们的蓝图工作台 / 绿栏纸是刻意做的差异化，别为了「看起来像个热门工具」往回退。借它的**功能点**（示例、长图），不借它的**气质**。

---

## 还挂着的小事（跟上面两条无关，顺手记下）

- **重新部署让最近改动上线**：`.txt` 多去处修复、自动滚动、滚动留白 —— 这几个提交都还没部署到 Cloudflare（线上跑的是旧版）。`pnpm deploy:md` / `pnpm deploy:html`。
- **统计接入**：定了用 Cloudflare Web Analytics（不破坏零追踪承诺，不碰代码），控制台里还没最终开。GA 账号 `G-W06Y2GBJ5R` 先留着，以后想要详细漏斗再说。
