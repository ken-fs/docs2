# 项目设计规则（AGENTS.md）

这份规则管整个仓库，两个站都算。原文来自 `Ship/# 项目设计规则（AGENTS.md`，
搬进项目是为了让它跟代码一起走版本 —— 规则放在仓库外面，改了没人知道，
新来的人（或下一个 agent）也看不到。

文末「这个仓库里怎么落地的」一节是补的，记录每条规则在这儿的具体形态。
规则本身没动。

## 角色设定
你是一位资深独立设计师，专注于 "反主流" 的网页美学。
你鄙视千篇一律的 SaaS 模板，追求每个像素都有温度。

## ❌ 绝对禁止项

### 配色禁止
- 紫色/靛蓝色/蓝紫渐变（#6366F1、#8B5CF6）
- 纯平背景色（必须有噪点纹理或渐变）
- Tailwind 默认色板

### 布局禁止
- Hero + 三卡片布局
- 完美居中对齐
- 等宽多栏（必须不对称）

### 文案禁止
- 高深的专业名词和无意义的空话
- Lorem Ipsum 占位文本
- 被动语态和长句

### 组件禁止
- Shadcn/Material UI 默认组件（必须深度定制）
- Emoji 作为功能图标
- 线性动画（ease-in-out）

## ✅ 必须遵守项

### 文案风格
- 口语化，像朋友聊天
- 具体化，有数字和场景
- 可以幽默、自嘲、甚至挑衅
- 每句话不超过 15 个字

### 图片系统
- 图标：使用 Iconify 图标库（https://iconify.design）
- 占位图：使用 Picsum Photos（https://picsum.photos）
- 真实图片：使用 Pexels 搜索（https://www.pexels.com）
- 插画：使用 unDraw（https://undraw.co）

---

## 这个仓库里怎么落地的

两个站刻意长得不一样 —— 同一个人做的两个工具站，放一起要能一眼分出来，
不能让人以为点错了返回键。

**docstomd（greenbar，行式打印机连页纸）**：白纸 / 墨 / 松绿。`--paper`
`--ink` `--pine` `--marker`，定义在 `apps/docstomd/src/app/globals.css`。
签名元素是横向绿条纹 —— 每条带 72px（正文行高众数 24px × 3 行），跟真的
绿栏纸一样每三行换一次底色，透明度 0.30。噪点层钉在视口上（纸面纤维），条纹跟着内容滚
（纸动条纹才动）。便签条歪贴（`rotate(-0.8deg)`），按钮有 `press` 下沉手感。

锚点选绿栏纸的理由：这个站的产物是纯文本（Markdown、等宽字、进 git），
绿栏纸就是「机器印出来的纯文本」的物证。原来那套 letterpress（奶油底 +
Fraunces 高对比衬线 + 陶土色）浪漫化的是输入端的 Word 文档，而用户来这儿
是为了输出端 —— 而且它正好撞在当前 AI 生成设计三种典型默认长相的第一种上
（奶油底 ≈ #F4F1EA + 高对比衬线 + 陶土色）。撞的不是规则，是同质化。

**字体**：display 从 Fraunces 换成 Bricolage Grotesque（刻意画歪的
grotesque，字怀不对称、末端切角），正文仍是 IBM Plex Sans / Mono。宽度是
量出来的：H1 容器 618px、字号 54.4px 下，Fraunces 单行自然宽 853px、
Bricolage 856px，几乎重合，所以换完标题行数不变。Zilla Slab（789px）和
Newsreader（800px）也好看，但会把首页标题从 3 行压成 2 行 —— 那是变相改布局。

**docs2html（蓝图工作台）**：冷白纸 / 石墨 / 普鲁士蓝。`--sheet` `--graphite`
`--prussian`，定义在 `apps/docs2html/src/app/globals.css`。签名元素是双层
绘图网格：24px 细格 + 144px 粗格。144 不是随手取的 —— 内容栏实测 left 144 /
right 1296，都是 144 的整数倍，所以粗线正好压在内容栏边缘上，网格成了布局的
定位基准而不是页边装饰。改容器宽度或 padding 要跟着改这个数。卡片是正交的
图纸片（`plate corners`），不歪，跟 docstomd 的歪贴便签正好相反。

主色是普鲁士蓝 `#0052a0`（H=252）—— 氰版晒图（cyanotype，"blueprint" 这个词
的来源）显影出来就是这个蓝。它替掉了原来那个叫 `indigo` 的 `#3a57be`（H=268）：
旧值本身不违反禁令（规则点名的 `#6366F1` 是 H=277、`#8B5CF6` 是 H=293，方案
要求的 `#4F46E5` 也是 H=277，都比它靠紫得多，方案那个早就否掉了），但 H=268
跟满街的 SaaS 主色难分辨。往青里推 16 度、明度压两档之后才真的像晒出来的图纸。

**强调色从三个减到两个**：删掉了中间那个青（H=218），并进主色。它管的全是
「成功/确认」，而主色本身就是「跑通了、拿到干净 HTML」的颜色；更实际的理由是
青和普鲁士蓝差 34 度，同一页里两种蓝混着出现看着像没调准，而真的晒图上只有
一种蓝。琥珀留着 —— 它是唯一的失败色，跟蓝对立，不是第三种蓝。docstomd 那边
同理把 `moss` 并进了 `pine`。

背景的两团角落柔光渐变也删了：真图纸上没有光晕，而「角落两团渐变」正是当前
AI 生成页面最容易认出的一处笔迹。双层网格同样不是纯平背景，但它说的是「这是
张纸」而不是「这是块屏幕」。

**失败状态不能借主色**。docstomd 原来的失败卡片是 `bg-pine/8` + `border-pine/45`，
连 ✗ 图标都是 `text-pine` —— 而松绿是这个站的成功色，成功和失败只差一个字形。
绿栏纸上一整列绿框，第一眼全像跑通了。补了 `--redline`（`#b12b35`，H=22，
校对笔的红），刻意避开 H≈45 那一带的陶土色：那是这个站换掉的旧主色，绕回去
等于把否掉的方向请回来。docs2html 那边的 `--amber` 本来就是干这个的。

**报错必须给出路，但不能给假出路**。拖错文件类型时，两个站都按扩展名先挡下来
（`accept` 只管系统选择器的过滤器，挡不住从 Finder 拖进来的文件，所以 JS 这边
要再查一遍），然后按「离用户最近」排三种去处：本站有专门页 → 只有兄弟站收 →
谁都不收。头两种给可点的链接，最后一种不给 —— 无处可去时递一个链接是骗人白跑
一趟，所以那句文案得自己说完整（不收什么，以及这个站收什么）。

去处是从 `TOOL_INPUT` 派生的（`pagesForExtension`），不是另抄一份清单，加工具页
自动就有。唯一手写的格式清单是 docs2html 的 `SIBLING_EXTENSIONS` —— 那是另一个
站的能力，这个包里查不到，改 docstomd 的工具页时要顺手改它。

**去处可能不止一个，而且都对 —— 判据是引擎，不是页数。**原来那个
`pageForExtension` 只返回第一个匹配的页，而 docs2html 的 `.txt` 有三页收，
是三个不同的引擎（Markdown / 纯文本 / CSV）。一个 `.txt` 里装的可能是这三样中的
任何一样，光看扩展名分不出来，所以那个唯一答案有三分之二的概率是错的：拿着逗号
分隔的 `.txt` 被指到 Markdown 页，转出来是一坨没有表格的段落，而 CSV 页就在旁边。
四页都实测过，指的全是同一页。

反过来 docstomd 的 `.docx` 也有三页收（docx / word / google-docs），但那三页是
**同一个引擎的三个入口**，转出来的东西一字不差 —— 列三个链接是拿三个同义词烦
用户。所以 `pagesForExtension` 按 `engine` 去重：同引擎 = 别名，只留 `PAGE_KEYS`
里最靠前的那页；不同引擎 = 真歧义，全列出来让用户自己认哪个是他的文件。

去处不止一个时文案要换（`wrongTypeAmbiguous`）。`wrongType` 那句是「这一页不收，
那一页收」，后面跟三个链接读起来是错的（哪一页？），而且它没说清为什么有三个。
两个站都加了这条 —— docstomd 今天没有真歧义的扩展名（每个都只对应一个引擎），
但加一个收 `.txt` 的纯文本页就会造出来，那时候会出问题的地方不是派生逻辑，
是文案。两站保持同一套机制，别让下一个人重踩。

`hints` 是数组，不是单个。两站各有一条 spec 盯这件事（`.txt` 列全三条 /
同引擎折成一条），而且**断言必须锁到那张失败的任务卡上，不能用整个
`#convert`** —— 拖放区上方那行前置指路文案里有同样的链接，用 `#convert`
的话报错里一个链接都没有也照样绿。第一版就是这么写的，两条同时误报了。

**但上面那套报错只覆盖了两条入口里的一条。**`accept` 挡不住拖拽，所以拖错文件
有指路；而它*确实*挡得住系统选择器 —— 不收的格式在那儿是灰的，点不动，而灰掉的
文件永远进不了 `pick()`/`run()`，于是那三段报错一句都不会触发。走按钮的用户得到
的是彻底的静默。实际发生过：有人在 Markdown → HTML 页打开选择器，Word 文档是
灰的，没有任何解释，结论是「这站坏了」。两条入口的待遇正好反了 —— 拖进来的人
有指路，被过滤器挡住的人什么都没有。

**真正的修法是让选择器别撒谎**（`pickerAccept()`）：`<input accept>` 放行全站
任何一页能处理的格式，选中之后再由 `pick()`/`run()` 解释这个文件该去哪一页。
`input.accept` 仍然是本页的真实边界 —— 拖放区那行「本页收什么」
（`acceptSummary`）和扩展名预检都还用它，放宽的只是选择器那一处。

**为什么不是「在拖放区加一行指路文案」就够了**：第一次（Word）就是那么修的 ——
`elsewhereHints` 在按钮上方列出「.docx .doc → DOCX → HTML」。然后同一个人在
CSV 页又被 `.xlsx` 挡了一次。人是先点按钮再读字的，那行字在按钮正上方也拦不住
他。指路文案留着（对读字的人有用，也是站内链接），但它是补充，不是修复。

`elsewhereHints` 那两点容易写错：本页自己收的扩展名不算「别处」（Markdown 页
收 .txt，不该推人去纯文本页转一个这里就能转的文件）；docs2html 的 .pdf 全站一页
都不收，所以它根本不在 `elsewhere` 里，要单独从 `SIBLING_EXTENSIONS` 补 —— 而
「不能传 PDF 吗」恰恰是被问过的那个问题，只列站内去处等于把唯一真有答案的格式
漏掉。

两个站的 spec 各有两条盯这件事：一条盯选择器放行的格式（`the picker offers
every format the site can handle`），一条盯前置的指路文案。两条都要，它们测的是
两条不同的入口 —— 只测文案的话，选择器再撒一次谎照样绿。

**`verify/i18n.mjs` 要放过纯扩展名串。**`.md .markdown .txt .mdown` 有 18 个
拉丁字母，远高于 `MIN_LETTERS` 门槛，会被当成一整句漏翻报出来 —— 而扩展名六种
语言必须逐字相同，日语用户的 Word 后缀也还是 `.docx`。判据是
`EXTENSIONS_ONLY`（整条就是点开头的词），只放过纯扩展名串，`Other formats:`
和「句子里带扩展名」照查。门槛不能往上调，那会把真正短的句子一起放过。

同一张卡片里主色只能有一个意思：能点。所以报错正文改成中性的 `ink-soft` /
`graphite-soft`，主色只留给那条链接。

**没有这道检查之前是静默出垃圾**：拖一个 PDF 进 CSV 页，转换器把二进制当文本
硬解，输出一张全是乱码的表格，标成「成功」，还给下载。docx / pdf / xlsx 三个
引擎会自己嗅文件头拒掉，但 csv / html / markdown 走纯文本，什么都吃得下 ——
漏的正是那几页。起因是有人看着首页那行「Drop a file here / 25 MB per file」
问「只能传 txt 吗，不能 pdf、docx 吗」：那行字只说了大小和隐私，没说本页收
什么。现在它前面加了一段扩展名，从 `accept` 派生（`acceptSummary`），所以不会
跟选择器走散，也不用翻译六遍。代价是 `accept` 里扩展名的先后从此有语义 ——
按用户熟悉度排，别名放最后，因为那行字截到三个。

**两站的背景纹理要在同一个量级。**条纹原来是 0.40，单看这个站是对的，跟
docs2html 并排就压得明显重 —— 一边像做完的页面，另一边像草稿。量了一次才知道
原因不在单像素：条纹带色跟纸的亮度差 0.096，网格粗线是 0.150，像素上反而是网格
更重；但条纹盖满 50% 页面，网格只有 9.5%（24px 一根 1px 线加 144px 一根），
面积加权之后条纹是网格的 5.5 倍。降到 0.30 之后是 4.1 倍。

没有拉平到 1 倍：条纹和网格分别是两个站的签名元素，本来就该是页面上最认得出的
东西，拉平等于把两个签名都抹掉。再往下 0.2 那档截图比过，已经快看不出绿了，
就回到「认不出是印刷品」那个否掉过的位置。

**对比度是硬约束，改色板前先跑 `pnpm verify:contrast`。**它把两个站 25 个
页面里 header/main/footer 所有含文字的元素按 WCAG AA 判一遍（正文 4.5:1，
大字 3:1）。写它的时候就抓到两个既存缺陷：docstomd 的 `--ink-faint` 在纸上
只有 3.98、docs2html 的 `--graphite-faint` 只有 3.87，两个都不达标，而 112+115
条 Playwright 和 Lighthouse 都没报 —— 前者不查颜色，后者只抽查首屏。都修了。

**它有个盲区：只走文字节点。**图标是 SVG，走不到。docs2html 的 `--amber`
（失败图标）当时在纸上只有 1.98:1，连图形的 3:1 门槛都不到，是拿
`pnpm verify:color` 手算才发现的。挑色值用 `verify:color`（纯算，能算还没
写进 CSS 的候选值），验落地用 `verify:contrast`（跑真页面，要先 build 再起
两个 serve.mjs）。两个配着用。

主色的明度会被两头夹住：既要在纸上当正文色，又要当填充块配白字。docstomd 的
`--pine` 取 L=0.487 就是两边都过 4.5 的位置，往上调一档 white-on-pine 就掉下来。
两个 `globals.css` 末尾各有一张实测对比度表，改值时一并更新。

**缓动**：三条曲线全部带过冲或急停，`ease-in-out` 只出现在禁止它的那句注释里。
- `--snap: cubic-bezier(0.16, 1.06, 0.24, 1.04)`
- `--thud: cubic-bezier(0.36, 0, 0.12, 1)`
- `--spring: cubic-bezier(0.34, 1.56, 0.42, 1)`

**图标**：Iconify 的 Phosphor 集（`@iconify-json/ph`），但**不走运行时 CDN**。
`scripts/build-icons.mjs` 在构建时把用到的图标烘成 `src/components/icon-data.ts`
里的本地子集。原因是这两个站承诺「文件不上传、断网也能用」，而运行时向
iconify CDN 取图标就是往外发请求 —— 承诺会漏。没有用 emoji 当功能图标。

**占位图/真实图片/插画那三条在这里用不上**：两个站没有一张图片。工具站的主体是
转换器和产物预览，塞图只会挤掉首屏。所以也没有 Picsum/Pexels/unDraw 的依赖。
这不是漏掉了规则，是这类页面本来不需要。

**文案**：六种语言全是人写的（先写英文再翻译），没有 Lorem Ipsum，没有机翻。
「每句话不超过 15 个字」这条按精神执行 —— 短句、口语、给具体数字（「25 MB」
「几百毫秒」「拖四十个进来也会排队跑完」）。FAQ 的答案会有两三句连起来，
单句仍然短，但整条不止 15 字：那儿要把「为什么」讲清楚，硬切成一句会变成
它自己禁止的那种空话。

**组件**：shadcn 的组件全部改过 —— 不是换个圆角，是重写视觉语言（凸版下沉、
图纸描角、墨块 Indicator）。两个站的 `components/ui/` 各有一套，不共用。

**布局**：hero 是 12 栏里 7/5 的不对称分栏，不是居中三卡片。

## 长尾内容页（`/guides/`）

每站六篇，六语言，路由在 `[[...segments]]` 里作为 `Route` 判别联合的一支
（`{kind: "guide", slug}` / `{kind: "guideIndex"}`）。用 `/guides/` 不用
`/blog/`：这些是常青的格式说明，不按时间排，`/blog/` 会让人等更新。

**docstomd 是 7 个工具页配 6 篇。**`docx-to-markdown` 和 `word-to-markdown`
是同一个转换器，各写一篇就是两页抢同一批词。Word 那篇挂在两个工具页上。

**不认识的 slug 必须真 404，不能回退到列表页。**回退等于每个拼写错误都变成
一份重复内容的软 404。超过三段路径同理。两个站的 spec 各有四条盯这个。

**加了一段新路由，要同时加进五份页面清单**：`verify/i18n.mjs`、
`verify/contrast.mjs`、`verify/cross-site.mjs`，和两个站的 `site.spec.ts`
（后者加进 `ALL_SLUGS`，sitemap 那条断言会数条数）。五份都是手写数组，没有
自动发现。加完之前它们照样报绿 —— 绿的是「已列出的那些页面没问题」，不是
「全站没问题」。这一条是本项目最容易漏的地方：铺完 72 个页面之后跑全套，
四个检查全绿，读了源码才发现一个新页面都没查。

**`<pre>` 里的内容六种语言必须逐字相同。**那是转换器的真实输入和输出，
把 `| Part | Qty |` 翻成 `| 零件 | 数量 |` 就不再是这个转换器会吐出的东西，
而样例的全部价值就在于它是真的。`verify/i18n.mjs` 排除整棵 `<pre>` 子树
（`el.tagName === "PRE" || el.closest("pre")`）—— 叶子文字在嵌套的 `<code>`
上，只跳过 `<pre>` 这一个节点等于什么都没排除。

**两站的镜像文章最容易互抄。**六篇是刻意对着写的（Word 对 Word，CSV 对 CSV），
讲的又是同一批客观事实 —— 25 MB、10 万格、合并格没有 colspan。先写英文再翻译
这条流水线会把抄袭复制六遍，所以 `verify:cross` 六种语言全查。实际发生过一次：
三对英文页面超标，es/pt 跟着继承。修法不是换措辞，是换角度 —— docstomd 讲
Markdown 的限制（纯文本没有字体、一行必须是一行、没有 colspan），docs2html
讲 HTML 的（mso- 样式、`<h2>` 拿到站点样式、colspan 能表达）。同一件事换个词
说仍然是同一页，方案 §8.7 要的是每页有它自己的角度。

## 部署（Cloudflare Workers，不是 Pages）

两个站是两个独立的 Worker（Static Assets），配置在 `apps/*/wrangler.jsonc`。方案原文写的是 Pages —— 改成 Workers 是因为 Cloudflare 现在主推它、Pages 在维护状态；产物是同一堆静态文件，两边都发得出去。

**代价是 Pages 白给的两条行为在这儿要手写**，而且默认值跟这个站正好对不上，配错了**不报错**，只会静默变坏：

- `html_handling: "force-trailing-slash"` —— 站点是 `trailingSlash: true`，canonical 和 hreflang 写的全是带斜杠的地址。默认的 `auto-trailing-slash` 让不带斜杠的地址直接 200，于是同一个页面有两个都能打开的地址，权重白白分掉。
- `not_found_handling: "404-page"` —— `out/404.html` 必须以 404 状态码发。默认 `none` 发一个裸 404；`single-page-application` 更糟，会把每个打错的地址变成状态码 200 的重复内容，而「认不出的路径必须真 404」是这个项目已经花过力气的一条（`parseSegments` 返回 null → `notFound()`，两站各四条 spec 盯着）。

**尾斜杠跳转是 307，不是 308。**原来 `serve.mjs` 和两站的 spec 都写 308（凭「永久重定向更适合规范化跳转」的直觉），而实测 `wrangler dev`（真 workerd）给的是 307，官方 html-handling 文档的表格里也只有 307。三处都改成 307 了 —— 写错的代价不是测试报红，是**本地验收在这一条上撒谎**，而 `serve.mjs` 存在的全部意义就是「本地测出来的行为等于线上」。

验配置不用真部署：`wrangler deploy --dry-run --config apps/<app>/wrangler.jsonc` 会解析配置并数出 assets 文件数；要验路由行为就 `wrangler dev --config ... --port 8788 --local`，那是真 workerd，`curl -I` 一遍尾斜杠和 404 就知道。

## 别的注意事项

Next.js 版本较新，API 和文件结构可能跟训练数据不一样，写代码前先读
`node_modules/next/dist/docs/` 里对应的指南。`apps/docstomd/AGENTS.md`
里有同样的提醒（那份是工具生成的，带 BEGIN/END 标记，别手改）。
