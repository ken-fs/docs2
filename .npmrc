# eslint-config-next 内部 require("next/dist/compiled/babel/eslint-parser")，
# 而 next 只是 apps/* 的直接依赖，不会被 pnpm 提到仓库根。这里单独把 next
# 提上来，否则 monorepo 里 pnpm lint 直接报 Cannot find module。
public-hoist-pattern[]=next
